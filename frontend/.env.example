# ── ตัวอย่าง .env.local สำหรับ frontend ──
# คัดลอกเป็น `.env.local` แล้วแก้ตามจริง (ห้าม commit .env.local)

# Backend API base URL (Vite จะอ่านผ่าน import.meta.env.VITE_API_URL)
VITE_API_URL=http://localhost:8000
VITE_API_PREFIX=/api/v1

# (ภายหลังเพิ่ม: FEATURE FLAGS, SENTRY DSN, ฯลฯ)
